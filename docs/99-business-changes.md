# 🔄 Business Changes & New Requirements - Graphic School (Updated)

## 1. متطلبات أساسية (Core System Requirements)

1. النظام يدعم **أكثر من لغة** (Multi-language):
    - كل النصوص في الواجهة الأمامية (Website + Dashboard) تأتي من قاعدة البيانات أو من نظام الترجمة.
    - يمكن للـ Admin من لوحة التحكم:
        - إضافة لغة جديدة.
        - تفعيل/إلغاء تفعيل لغة.
        - تعديل ترجمة أي نص في النظام (Strings, Labels, Messages, etc.).

2. كل شيء في النظام مربوط بـ **صلاحيات (Permissions)**:
    - لكل Feature أو Action مهم Permission خاص به.
    - يمكن إنشاء Roles وربطها بمجموعة Permissions.
    - يمكن ربط كل User بواحد أو أكثر من Roles.
    - لا يمكن لطالب أو مدرب أو Admin رؤية أو تنفيذ أي شيء بدون Permission مناسب.

3. يجب أن يكون هناك **سجل Audit Log كامل**:
    - يسجل كل عمليات:
        - إنشاء (Create)
        - تعديل (Update)
        - حذف (Delete)
        - تغيير حالات مهمة (Status changes)
    - لكل عملية يتم تسجيل:
        - الـ User الذي قام بالفعل
        - نوع العملية
        - الكيان المتأثر (Course, Session, User, Payment, Certificate, etc.)
        - التاريخ والوقت
        - القيمة القديمة والجديدة (لو أمكن)
    - يكون هناك صفحة في لوحة التحكم للـ Admin:
        - لعرض الـ Logs
        - مع فلترة حسب:
            - المستخدم
            - نوع العملية
            - التاريخ
            - نوع الكيان

---

## 2. Website CMS (Page Builder & Content Management)

الموقع الخارجي لازم يكون **قابل للتحكم بالكامل** من لوحة التحكم:

### 2.1 إدارة الصفحات (Page Builder)
- الصفحات الأساسية:
    - الصفحة الرئيسية (Home)
    - عن الشركة (About)
    - الكورسات (Courses Listing)
    - تفاصيل الكورس (Course Details)
    - المدربين (Instructors Listing)
    - تفاصيل المدرب (Instructor Profile)
    - تواصل معنا (Contact)
- لكل صفحة، يمكن للـ Admin:
    - التحكم في العناوين والنصوص (Texts)
    - التحكم في الصور (Images)
    - التحكم في الألوان (Colors/theme settings إن كانت موجودة)
    - إظهار/إخفاء سكاشن (Sections) معينة في الصفحة:
        - Slider
        - Testimonials
        - Featured Courses
        - Statistics
        - FAQ
        - أي سكاشن أخرى موجودة

### 2.2 إدارة الصور (Media / Images)
- نظام لإدارة الصور (Media Library):
    - رفع صور
    - مسح/تعديل بيانات الصور
    - إعادة استخدامها في السكاشن المختلفة.

### 2.3 Slider Manager
- إمكانية إدارة الـ Slider في الصفحة الرئيسية:
    - إضافة/حذف/تعديل Slides
    - لكل Slide:
        - عنوان
        - وصف
        - صورة
        - زر CTA (رابط + نص)
        - ترتيب الظهور
    - إظهار/إخفاء الـ Slider بالكامل.

### 2.4 Testimonials
- إدارة التقييمات Testimonials التي تظهر في الموقع:
    - إضافة/تعديل/حذف Testimonial
    - ربطها بمدرب/كورس أو شخص عام
    - اختيار أي Testimonials تظهر على الصفحة الرئيسية.

### 2.5 FAQ
- إدارة أسئلة وأجوبة متكررة:
    - سؤال
    - جواب
    - ترتيب
    - إظهار/إخفاء

### 2.6 Contact Settings & Social Links
- إعدادات التواصل:
    - Email
    - Phone
    - Address
    - Location map (اختياري)
- روابط السوشيال:
    - Facebook
    - Instagram
    - LinkedIn
    - YouTube
    - إلخ…

### 2.7 SEO
- لكل صفحة من الصفحات:
    - Title
    - Meta Description
    - Keywords (اختياري)
- التحكم في SEO من لوحة التحكم.
- إمكانية تحديد لغة الـ SEO لكل لغة من لغات الموقع (Multi-language SEO).

---

## 3. Notifications System & Notification Center

### 3.1 نظام إشعارات متكامل
- أنواع الإشعارات (على الأقل):
    - إشعار قبول/رفض التسجيل في كورس.
    - إشعار إضافة الطالب إلى دفعة معينة (Batch).
    - إشعار بمواعيد المحاضرات القادمة.
    - إشعار بوجود واجب/امتحان جديد.
    - إشعار بنتائج الامتحانات.
    - إشعار بتحديث حالة الدفع (دفعة تمت / متأخرة / مرفوضة).
- القنوات (في البداية):
    - In-App Notifications (داخل النظام).
    - إمكانية توسيعها لاحقاً (Email / SMS).

### 3.2 Notification Center
- لكل مستخدم (Student / Instructor / Admin):
    - صفحة أو Dropdown للـ Notifications.
    - يمكنه:
        - رؤية كل الإشعارات.
        - فلترة (غير مقروء / مقروء).
        - Mark as read.
- للـ Admin:
    - رؤية إشعارات مخصصة مثل:
        - طلبات تسجيل جديدة.
        - طلبات دعم (Tickets) جديدة.

---

## 4. Payments & Payment Timeline

### 4.1 Timeline للدفعات (Payment Timeline)
- لكل طالب ولكل كورس/دفعة:
    - تسجيل كل دفعة دفعها الطالب:
        - المبلغ المدفوع
        - التاريخ
        - طريقة الدفع (إن وجدت)
        - وصف (اختياري)
    - تسجيل المتبقي من المبلغ (Remaining).
- الواجهة للطالب:
    - رؤية جميع الدفعات التي دفعها.
    - رؤية المتبقي عليه.
    - رؤية حالة الدفع (Paid / Partially Paid / Pending).
- الواجهة للـ Admin:
    - إدارة كل دفعات الطلاب.
    - تعديل حالة الدفع.
    - إضافة دفعات يدوياً.
    - تقارير عن:
        - إجمالي المدفوع
        - المتأخر
        - حسب batch أو course.

---

## 5. Messaging System (Student ⇄ Instructor)

### 5.1 نظام رسائل (Chat بسيط وليس Live Presence)
- بين الطالب والمدرب فقط (أو بين الطالب والمدربين المرتبطين بالكورسات التي يدرسها).
- ليس نظام Live chat أو Real-time presence، لكن:
    - شات بسيط (Inbox/Thread):
        - الطالب يمكنه إرسال رسالة إلى المدرب (أو على مستوى الكورس/المحاضرة).
        - المدرب يمكنه الرد.
    - كل محادثة مرتبطة ب:
        - كورس معين (Course)
        - أو محاضرة معينة (Session) إن أمكن.
- يجب أن يكون هناك:
    - قائمة محادثات للطالب.
    - قائمة محادثات للمدرب.
    - Notifications عند وصول رسالة جديدة.

---

## 6. Ticketing System (Admin ⇄ Technical Company)

### 6.1 تذاكر الدعم بين Admin والشركة التقنية
- هذا النظام **ليس للطلاب أو المدربين**.
- المستخدم الوحيد هنا من جهة المنصة: **Admin / Super Admin**.
- Admin يفتح Ticket مع الشركة التقنية التي أنشأت المشروع:
    - يختار:
        - نوع الطلب:
            - مشكلة (Bug)
            - تغيير (Change Request)
            - طلب جديد (New Feature)
        - يكتب الوصف بالتفصيل.
        - يمكنه رفع صورة/ملف مرفق.
    - يرى حالة الطلب:
        - Open
        - In Progress
        - Resolved
        - Closed
    - يمكنه رؤية تاريخ التحديثات على الـ Ticket.
- الطرف الآخر (الشركة التقنية) قد يتعامل من نظام خارجي أو من نفس النظام (حسب التنفيذ)، لكن من ناحيةنا:
    - واجهة كاملة لـ Admin لمتابعة كل التذاكر المفتوحة والتاريخية.

---

## 7. Advanced Reports & Analytics

### 7.1 تقارير تفصيلية (للـ Admin & Instructor)

#### للـ Admin:
- أفضل الطلاب (Top Students):
    - حسب درجاتهم.
    - حسب نسبة حضورهم.
    - حسب نشاطهم (عدد المحاضرات/الاختبارات/المشاريع المكتملة).
- متوسط الدرجات (Average Grades):
    - لكل كورس.
    - لكل دفعة.
    - لكل مدرب.
- نسبة الحضور (Attendance Rate):
    - لكل كورس.
    - لكل دفعة.
    - لكل طالب.
- جودة التفاعل (Engagement Quality):
    - عدد الأسئلة.
    - عدد التعليقات.
    - عدد الملفات المرفوعة.

#### للـ Instructor:
- تقارير عن:
    - عدد الطلاب في كل كورس/دفعة.
    - متوسط درجات الطلبة في الكورس.
    - نسبة حضور الطلاب في كورساتهم.
    - تقييم عام لأداء المدرب مبني على:
        - تقييمات الطلاب.
        - نسب النجاح.
        - الحضور.

---

## 8. Multi-language Dynamic UI

- كل ما يظهر في الواجهة (Website + Dashboard) يكون معتمد على:
    - نظام ترجمة يدعم:
        - Keys (مثال: `courses.title`, `home.hero_title`, etc.)
        - قيم لكل لغة.
- الـ Admin يمكنه:
    - إضافة لغة جديدة.
    - تعديل ترجمة أي Key من خلال شاشة ترجمة.
- يجب تحديث أي جزء في الكود يستخدم نص ثابت (Hardcoded) إلى:
    - ترجمة ديناميكية من DB أو من Translation Layer.

---

## 9. Cleanup & Deprecations

يجب إزالة أو وضع Deprecated على أي شيء من التالي (إن وجد بالكود الحالي):

1. أي Feature لا يستخدمه:
    - الطالب
    - المدرب
    - الـ Admin
2. أي جزء LMS عام لا علاقة له بمجال الجرافيك/التدريب الحالي (إن وجد، مثل Features قديمة غير مستخدمة).
3. أي جداول غير مستخدمة في قاعدة البيانات.
4. أي API Endpoints غير مستعملة من الFrontend أو غير موثقة في `/docs/12-api-docs.md`.
5. أي Modules تجريبية أو غير مكتملة لا تدخل في السيناريو الحالي.

---

## 10. Permissions (RBAC) Summary

- لكل Feature أو مجموعة Features رئيسية يجب أن يضاف Permission واضح، مثل:
    - `view_courses`, `manage_courses`
    - `view_students`, `manage_students`
    - `view_attendance`, `manage_attendance`
    - `view_payments`, `manage_payments`
    - `view_reports`
    - `view_cms_pages`, `manage_cms_pages`
    - `view_translations`, `manage_translations`
    - `view_notifications`, `manage_notifications`
    - `view_tickets`, `manage_tickets`
    - إلخ…

- يجب التأكد من:
    - أن كل شاشة / Endpoint / Action في النظام مربوطة بصلاحية مناسبة.
    - Roles:
        - Student
        - Instructor
        - Admin
        - Super Admin (لو موجود)
    - يمكن توسيع الـ Roles لاحقاً.

---

## 11. ملاحظات عامة

- كل التغييرات السابقة يجب أن:
    - تحترم البنية المعمارية الحالية (Modular Monolith + DDD).
    - لا تكسر أي Features موجودة حالياً تعمل بشكل صحيح.
    - يتم توثيقها في:
        - Product Scope
        - Feature List
        - User Stories
        - Use Cases
        - API Docs
        - System Flows
        - وغيرها من ملفات `/docs` حسب الحاجة.
