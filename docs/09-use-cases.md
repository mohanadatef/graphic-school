# 🎯 Use Cases - Graphic School

## Use Cases مفصلة للعمليات الرئيسية

---

## UC-001: تسجيل مستخدم جديد

### الممثل الأساسي:
مستخدم جديد (Student)

### السيناريو الرئيسي (Main Flow):
1. المستخدم يفتح صفحة التسجيل
2. يملأ النموذج:
   - الاسم
   - البريد الإلكتروني
   - كلمة المرور
   - تأكيد كلمة المرور
   - رقم الهاتف (اختياري)
   - العنوان (اختياري)
3. يضغط على "إنشاء حساب"
4. النظام يتحقق من صحة البيانات:
   - البريد الإلكتروني فريد
   - كلمة المرور قوية (8+ أحرف)
   - تأكيد كلمة المرور يطابق
5. النظام ينشئ حساب جديد
6. النظام يعين دور "student" افتراضياً
7. النظام ينشئ Token
8. النظام يعيد User + Token
9. المستخدم يتم تسجيل دخوله تلقائياً
10. يتم توجيهه لـ Dashboard

### السيناريوهات البديلة (Alternate Flows):

#### A1: البريد الإلكتروني موجود مسبقاً
- 4a. النظام يكتشف أن البريد موجود
- 4b. يعيد رسالة خطأ: "البريد الإلكتروني مستخدم بالفعل"
- 4c. يعود للخطوة 2

#### A2: كلمة المرور ضعيفة
- 4a. النظام يكتشف أن كلمة المرور ضعيفة
- 4b. يعيد رسالة خطأ: "كلمة المرور يجب أن تكون 8 أحرف على الأقل"
- 4c. يعود للخطوة 2

#### A3: تأكيد كلمة المرور لا يطابق
- 4a. النظام يكتشف عدم التطابق
- 4b. يعيد رسالة خطأ: "تأكيد كلمة المرور لا يطابق"
- 4c. يعود للخطوة 2

### أخطاء محتملة:
- **Validation Error**: بيانات غير صحيحة
- **Server Error**: خطأ في الخادم
- **Network Error**: مشكلة في الاتصال

---

## UC-002: تسجيل الدخول

### الممثل الأساسي:
مستخدم (Admin, Instructor, Student)

### السيناريو الرئيسي:
1. المستخدم يفتح صفحة تسجيل الدخول
2. يملأ البريد الإلكتروني وكلمة المرور
3. يضغط على "تسجيل الدخول"
4. النظام يتحقق من البيانات
5. النظام يتحقق من وجود المستخدم
6. النظام يتحقق من كلمة المرور
7. النظام ينشئ Token جديد
8. النظام يعيد User + Token
9. يتم حفظ Token في localStorage
10. يتم توجيه المستخدم لـ Dashboard حسب دوره

### السيناريوهات البديلة:

#### A1: البريد الإلكتروني غير موجود
- 5a. النظام لا يجد المستخدم
- 5b. يعيد رسالة خطأ: "بيانات الدخول غير صحيحة"
- 5c. يعود للخطوة 2

#### A2: كلمة المرور خاطئة
- 6a. النظام يكتشف أن كلمة المرور خاطئة
- 6b. يعيد رسالة خطأ: "بيانات الدخول غير صحيحة"
- 6c. يعود للخطوة 2

### أخطاء محتملة:
- **401 Unauthorized**: بيانات خاطئة
- **429 Too Many Requests**: محاولات كثيرة
- **500 Server Error**: خطأ في الخادم

---

## UC-003: إنشاء كورس جديد

### الممثل الأساسي:
Admin

### السيناريو الرئيسي:
1. Admin يفتح صفحة "إدارة الكورسات"
2. يضغط على "إضافة كورس جديد"
3. يملأ النموذج:
   - العنوان
   - الكود (Code)
   - التصنيف
   - الوصف
   - السعر
   - تاريخ البدء
   - عدد الجلسات
   - أيام الأسبوع
   - الأوقات (بداية، نهاية)
   - نوع التسليم (On-site, Online, Hybrid)
   - عدد الطلاب الأقصى
   - صورة (اختياري)
4. يضغط على "حفظ"
5. النظام يتحقق من صحة البيانات
6. النظام ينشئ Course جديد
7. النظام يحفظ الصورة (إن وجدت)
8. النظام يعيد Course مع ID
9. Admin يرى رسالة نجاح
10. يتم توجيهه لصفحة الكورس

### السيناريوهات البديلة:

#### A1: الكود موجود مسبقاً
- 5a. النظام يكتشف أن الكود موجود
- 5b. يعيد رسالة خطأ: "الكود مستخدم بالفعل"
- 5c. يعود للخطوة 3

#### A2: التصنيف غير موجود
- 5a. النظام يكتشف أن التصنيف غير موجود
- 5b. يعيد رسالة خطأ: "التصنيف غير موجود"
- 5c. يعود للخطوة 3

### أخطاء محتملة:
- **422 Validation Error**: بيانات غير صحيحة
- **500 Server Error**: خطأ في الخادم
- **File Upload Error**: خطأ في رفع الصورة

---

## UC-004: التسجيل في كورس

### الممثل الأساسي:
Student

### السيناريو الرئيسي:
1. Student يفتح صفحة تفاصيل كورس
2. يرى معلومات الكورس (السعر، الجلسات، إلخ)
3. يضغط على "سجل الآن"
4. النظام يتحقق من:
   - الكورس منشور
   - الكورس غير مخفي
   - الطالب غير مسجل مسبقاً
5. النظام ينشئ Enrollment بحالة "pending"
6. النظام يعيد Enrollment
7. Student يرى رسالة: "تم التسجيل بنجاح، في انتظار الموافقة"
8. Admin يتلقى إشعار بتسجيل جديد

### السيناريوهات البديلة:

#### A1: الكورس غير منشور
- 4a. النظام يكتشف أن الكورس غير منشور
- 4b. يعيد رسالة خطأ: "الكورس غير متاح حالياً"
- 4c. يعود للخطوة 1

#### A2: الطالب مسجل مسبقاً
- 4a. النظام يكتشف أن الطالب مسجل مسبقاً
- 4b. يعيد رسالة خطأ: "أنت مسجل في هذا الكورس بالفعل"
- 4c. يعود للخطوة 1

#### A3: الكورس ممتلئ
- 4a. النظام يكتشف أن عدد المسجلين وصل للحد الأقصى
- 4b. يعيد رسالة خطأ: "الكورس ممتلئ"
- 4c. يعود للخطوة 1

### أخطاء محتملة:
- **422 Validation Error**: بيانات غير صحيحة
- **409 Conflict**: مسجل مسبقاً
- **403 Forbidden**: غير مصرح

---

## UC-005: الموافقة على التسجيل

### الممثل الأساسي:
Admin

### السيناريو الرئيسي:
1. Admin يفتح صفحة "التسجيلات"
2. يرى قائمة التسجيلات المعلقة
3. يضغط على تسجيل معين
4. يرى تفاصيل التسجيل (الطالب، الكورس، التاريخ)
5. يضغط على "موافقة"
6. النظام يحدث حالة Enrollment إلى "approved"
7. النظام يسمح للطالب بالحضور (can_attend = true)
8. النظام يسجل Admin الذي وافق والوقت
9. Student يتلقى إشعار بالموافقة
10. Admin يرى رسالة نجاح

### السيناريوهات البديلة:

#### A1: رفض التسجيل
- 5a. Admin يضغط على "رفض"
- 5b. النظام يحدث حالة Enrollment إلى "rejected"
- 5c. Student يتلقى إشعار بالرفض
- 5d. Admin يمكنه إضافة ملاحظة للرفض

### أخطاء محتملة:
- **404 Not Found**: التسجيل غير موجود
- **422 Validation Error**: بيانات غير صحيحة
- **500 Server Error**: خطأ في الخادم

---

## UC-006: إجراء Quiz

### الممثل الأساسي:
Student

### السيناريو الرئيسي:
1. Student يفتح كورس
2. يرى Quiz متاح
3. يضغط على "ابدأ Quiz"
4. النظام يتحقق من:
   - الطالب مسجل في الكورس
   - Quiz منشور
   - لم يتجاوز عدد المحاولات الأقصى
5. النظام ينشئ QuizAttempt
6. النظام يبدأ Timer (إن وجد)
7. Student يرى الأسئلة
8. Student يجيب على الأسئلة
9. Student يضغط على "تقديم"
10. النظام يحسب النتيجة
11. النظام يحفظ QuizAttempt مع النتيجة
12. Student يرى النتيجة (إن كان مسموحاً)

### السيناريوهات البديلة:

#### A1: انتهى الوقت
- 6a. Timer يصل للصفر
- 6b. النظام يقدم Quiz تلقائياً
- 6c. النظام يحسب النتيجة
- 6d. Student يرى النتيجة

#### A2: تجاوز عدد المحاولات
- 4a. النظام يكتشف أن الطالب تجاوز عدد المحاولات
- 4b. يعيد رسالة خطأ: "لقد تجاوزت عدد المحاولات المسموح"
- 4c. يعود للخطوة 2

### أخطاء محتملة:
- **403 Forbidden**: غير مصرح
- **422 Validation Error**: بيانات غير صحيحة
- **500 Server Error**: خطأ في الخادم

---

## UC-007: تسجيل الحضور

### الممثل الأساسي:
Instructor

### السيناريو الرئيسي:
1. Instructor يفتح جلسة
2. يرى قائمة الطلاب المسجلين في الكورس
3. يسجل حضور/غياب لكل طالب
4. يضغط على "حفظ"
5. النظام ينشئ/يحدث سجلات Attendance
6. النظام يحفظ الحضور
7. Instructor يرى رسالة نجاح
8. الطلاب يمكنهم رؤية حالات حضورهم

### السيناريوهات البديلة:

#### A1: طالب غير مسجل
- 3a. Instructor يحاول تسجيل حضور لطالب غير مسجل
- 3b. النظام يرفض
- 3c. يعيد رسالة خطأ: "الطالب غير مسجل في هذا الكورس"

### أخطاء محتملة:
- **403 Forbidden**: غير مصرح
- **422 Validation Error**: بيانات غير صحيحة
- **500 Server Error**: خطأ في الخادم

---

## UC-008: توليد جلسات تلقائياً

### الممثل الأساسي:
Admin

### السيناريو الرئيسي:
1. Admin يفتح كورس
2. يضغط على "توليد جلسات"
3. النظام يقرأ إعدادات الكورس:
   - تاريخ البدء
   - عدد الجلسات
   - أيام الأسبوع
   - الأوقات (بداية، نهاية)
4. النظام يحسب تواريخ الجلسات
5. النظام ينشئ Sessions تلقائياً
6. النظام يعيد قائمة الجلسات المنشأة
7. Admin يرى رسالة نجاح مع عدد الجلسات

### السيناريوهات البديلة:

#### A1: جلسات موجودة مسبقاً
- 4a. النظام يكتشف وجود جلسات
- 4b. يسأل Admin: "هل تريد حذف الجلسات القديمة وإنشاء جديدة؟"
- 4c. Admin يختار:
   - نعم: يحذف القديمة وينشئ جديدة
   - لا: يلغي العملية

### أخطاء محتملة:
- **422 Validation Error**: بيانات غير صحيحة (مثلاً: عدد الجلسات = 0)
- **500 Server Error**: خطأ في الخادم

---

## UC-009: إصدار شهادة

### الممثل الأساسي:
System (تلقائي)

### السيناريو الرئيسي:
1. Student يكمل كورس (100% إتمام)
2. النظام يتحقق من:
   - جميع Lessons مكتملة
   - جميع Quizzes مكتملة (إن وجدت)
   - جميع Projects مكتملة (إن وجدت)
3. النظام ينشئ Certificate تلقائياً
4. النظام يولد رقم شهادة فريد
5. النظام يولد كود تحقق فريد
6. النظام يحفظ Certificate
7. Student يتلقى إشعار بإصدار الشهادة
8. Student يمكنه عرض وتحميل الشهادة

### السيناريوهات البديلة:

#### A1: الكورس غير مكتمل
- 2a. النظام يكتشف أن الكورس غير مكتمل 100%
- 2b. لا يتم إصدار شهادة
- 2c. يعود للخطوة 1

### أخطاء محتملة:
- **500 Server Error**: خطأ في إنشاء الشهادة
- **File Generation Error**: خطأ في توليد PDF

---

**آخر تحديث**: 2025-11-21  
**الإصدار**: 1.0.0

