# 🧪 كيفية تشغيل اختبارات Cypress

## ⚠️ متطلبات قبل التشغيل

**يجب تشغيل Frontend Server أولاً!**

```bash
# Terminal 1: تشغيل Frontend
cd graphic-school-frontend
npm run dev
```

**يجب أن يعمل الخادم على:** `http://localhost:5173`

---

## تشغيل الاختبارات

### 1. تشغيل جميع الاختبارات
```bash
npm run cypress:run
```

### 2. تشغيل اختبار محدد
```bash
npx cypress run --spec cypress/e2e/health_check.cy.js
```

### 3. فتح Cypress UI
```bash
npm run cypress:open
```

---

## 🔍 فحص المشاكل

### المشكلة: جميع الاختبارات تفشل بسرعة

**السبب**: Frontend server غير شغال

**الحل**:
1. تأكد من تشغيل `npm run dev` في terminal منفصل
2. افتح `http://localhost:5173` في المتصفح للتأكد
3. ثم شغل الاختبارات

### المشكلة: "Connection refused" أو "ECONNREFUSED"

**السبب**: الخادم غير متاح على المنفذ 5173

**الحل**:
- تحقق من أن Vite يعمل
- تحقق من أن المنفذ 5173 غير مستخدم
- أعد تشغيل `npm run dev`

---

## 📊 النتائج

بعد تشغيل الاختبارات، يمكنك فحص:

- `cypress/e2e-logs/summary.json` - ملخص الاختبارات
- `cypress/e2e-logs/spec-results/*.json` - نتائج كل spec
- `cypress/e2e-logs/routes.log` - Routes visited
- `cypress/e2e-logs/i18n-missing.log` - Missing translation keys

---

## ✅ Checklist قبل التشغيل

- [ ] Frontend server يعمل (`npm run dev`)
- [ ] Backend API يعمل (اختياري للاختبارات الأساسية)
- [ ] المنفذ 5173 متاح
- [ ] المتصفح يمكنه الوصول إلى `http://localhost:5173`

